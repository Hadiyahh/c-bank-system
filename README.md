# c-bank-system
A basic bank system built in C for a class project. It lets you create and delete customers, handle simple transactions, and manage accounts using linked lists. Passwords are lightly encrypted, and each customer gets a unique ID.
